# Pinned yamaa engine: merge commit of elong0527/yamaa PR #759
# (engine fixes the Pilot 3 ADLBC spec relies on: intermediate match index,
# window group_by via readable row view, stable column dependency order).
#
# Installed editable so the engine's schema bundle (yaml/schema.yaml, at the
# repo root outside the Python package) is discoverable at runtime; a plain
# wheel install cannot find it.
-e git+https://github.com/elong0527/yamaa.git@1a7620e8#egg=yamaa&subdirectory=python
polars
pyarrow
